import Feed from '../(tabs)/feed';

// Reuses the existing Feed screen (per Phase 5A instructions: "Do NOT rebuild
// Feed" - only reconnect navigation). Providers see the same community feed
// as customers.
export default Feed;
